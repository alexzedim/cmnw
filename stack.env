# Worker Concurrency Settings
# Calculated based on: 5 OSINT keys (350 RPS), 1 DMA key (70 RPS)
# Adjust these values to control how many jobs each worker processes concurrently

# OSINT Workers
CHARACTERS_WORKER_CONCURRENCY=2
GUILDS_WORKER_CONCURRENCY=2
PROFILE_WORKER_CONCURRENCY=1

# DMA Workers
AUCTIONS_WORKER_CONCURRENCY=2
ITEMS_WORKER_CONCURRENCY=5

# Worker Replicas
OSINT_REPLICAS=5
DMA_REPLICAS=2
